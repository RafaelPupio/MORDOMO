# Secrets — this repo is public; nothing sensitive ever enters it
.env
.env.*
!.env.example
# NOTE: the Vercel CLI re-appends a bare `.env*` at the end of this file on
# `env pull`, which silently overrides the negation above. Delete it when it
# reappears; check with: git check-ignore -v --no-index .env.example

# Dependencies & build
node_modules/
.next/
.vercel/
out/
*.tsbuildinfo
next-env.d.ts

# OS / editor
.DS_Store
.obsidian/

# SDD scratch
.superpowers/

# Agent tooling artifacts (installed locally, not part of the product)
.agents/
skills-lock.json
.claude/skills/
.env*
